name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS
            os: macos-latest
          - name: Windows
            os: windows-latest
          - name: Linux
            os: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # macOS System Dependencies
      - name: Install macOS dependencies
        if: runner.os == 'macOS'
        run: |
          brew install ninja libyaml spdlog console_bridge tinyxml2 urdfdom eigen asio

      # Linux System Dependencies
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libyaml-dev libspdlog-dev libtinyxml2-dev libeigen3-dev libconsole-bridge-dev liburdfdom-dev libasio-dev

      # Windows System Dependencies & Configuration
      - name: Configure Windows environment
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          git config --system core.longpaths true
          vcpkg install libyaml tinyxml2 spdlog eigen3 asio console-bridge urdfdom --triplet x64-windows

      # Configure Superbuild
      - name: Configure Superbuild (POSIX)
        if: runner.os != 'Windows'
        run: |
          cmake -B build -S . \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/install"

      - name: Configure Superbuild (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake -B build -S . `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/install" `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"

      # Build Underlay
      - name: Build Underlay
        run: |
          cmake --build build --config Release --parallel 4

      # Downstream Verification: Build and run examples/minimal_consumer
      - name: Configure Downstream Example (POSIX)
        if: runner.os != 'Windows'
        run: |
          cmake -B build_example -S examples/minimal_consumer \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/install"

      - name: Configure Downstream Example (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake -B build_example -S examples/minimal_consumer `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/install" `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"

      - name: Build Downstream Example
        run: |
          cmake --build build_example --config Release

      - name: Run Downstream Example (POSIX)
        if: runner.os != 'Windows'
        run: |
          ./build_example/minimal_consumer

      - name: Run Downstream Example (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:PATH = "${{ github.workspace }}\install\bin;$env:PATH"
          .\build_example\Release\minimal_consumer.exe

      # Upload Prebuilt Underlay Archive
      - name: Package and Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: minimal_ros2-${{ matrix.name }}
          path: install/
          retention-days: 7
